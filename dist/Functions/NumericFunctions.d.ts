declare class NumericFunctionsFacade {
    numberFormat(number: number, decimals?: number, dec_point?: string, thousands_sep?: string): string;
    bytesFormat(bytes: number): string;
}
export declare const NumericFunctions: NumericFunctionsFacade;
export {};
